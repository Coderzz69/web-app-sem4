// index.ts

// A simple TypeScript function to greet the user
function greet(name: string): string {
    return `Hello, ${name}!`;
}

// Example usage
const userName = 'World';
console.log(greet(userName));

let myString : string = `this is "My String";`